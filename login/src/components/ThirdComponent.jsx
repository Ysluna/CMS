import React from 'react'

export const ThirdComponent = () => {
  return (
    <div>
        <label for="password"
        class="label-log">
        </label>
        <input
            class="user-log"
            id="password"
            name="password"
            type="password"
            placeholder="Contraseña"
            required></input>
    </div>
  )
}
